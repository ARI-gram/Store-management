<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Utilize Inventory</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: url('/images/image2.jpg') no-repeat center center fixed;
            background-size: cover;
            backdrop-filter: blur(8px);
            color: #fff;
        }

        h1 {
            text-align: center;
            color: #4CAF50;
            margin-top: 20px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
        }

        .content {
            width: 90%;
            max-width: 600px;
            margin: 50px auto;
            background-color: rgba(0, 0, 0, 0.7);
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        .inventory-details {
            margin-bottom: 20px;
            padding: 15px;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 5px;
        }

        .inventory-details p {
            margin: 5px 0;
            font-size: 14px;
        }

        .inventory-details span {
            font-weight: bold;
            color: #4CAF50;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        label {
            font-weight: bold;
            color: #4CAF50;
        }

        input, textarea, button {
            padding: 10px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
        }

        input, textarea {
            background-color: rgba(255, 255, 255, 0.9);
            color: #000;
        }

        input:focus, textarea:focus {
            outline: none;
            border: 2px solid #4CAF50;
        }

        button {
            background-color: #4CAF50;
            color: white;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #45a049;
        }

        @media screen and (max-width: 768px) {
            .content {
                width: 95%;
            }

            h1 {
                font-size: 24px;
            }

            input, textarea, button {
                font-size: 14px;
            }
        }
        
        /* Menu Bar Style */
        .menu-bar {
            background-color: #333;
            padding: 10px 0;
            text-align: center;
        }

        .menu-bar ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .menu-bar ul li {
            display: inline-block;
            margin-right: 20px;
        }

        .menu-bar ul li:last-child {
            margin-right: 0;
        }

        .menu-bar ul li a {
            color: white;
            text-decoration: none;
            font-size: 16px;
            padding: 8px 16px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .menu-bar ul li a:hover {
            background-color: #555;
        }
    </style>
</head>
<body>
    <div class="menu-bar">
        <ul>
            <li><a href="/dashboard/<%= storeId %>">Dashboard</a></li>
            <li><a href="/<%= storeId %>/inventory">Inventory</a></li>
            <li><a href="/<%= storeId %>/orders">Orders</a></li>
            <li><a href="/deliveries/<%= storeId %>">Deliveries</a></li>
        </ul>
    </div>
    <div class="content">
        <h1>Utilize Inventory</h1>
        
        <!-- Display Inventory Details -->
        <div class="inventory-details">
            <p><span>Code:</span> <%= item.code %></p>
            <p><span>Item:</span> <%= item.item %></p>
            <p><span>Description:</span> <%= item.description %></p>
            <p><span>Available Quantity:</span> <%= item.quantity %></p>
            <p><span>Units:</span> <%= item.units %></p>
        </div>

        <!-- Utilization Form -->
        <form action="/<%= storeId %>/inventory/utilize/<%= item.code %>" method="POST">
            <div>
                <label for="quantity">Quantity to Utilize:</label>
                <input type="number" id="quantity" name="quantity" required>
            </div>
            <div>
                <label for="units">Units:</label>
                <input type="text" id="units" name="units" required>
            </div>
            <div>
                <label for="comments">Utilization Comments:</label>
                <textarea id="comments" name="comments" placeholder="Enter how it was utilized, in point form..." required></textarea>
            </div>
            <button type="submit">Submit Utilization</button>
        </form>
    </div>
</body>
</html>
