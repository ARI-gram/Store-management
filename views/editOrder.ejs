<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Order</title>
    <style>
        /* General Styling */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
        }

        /* Content container */
        .content {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* Page Title */
        h1 {
            text-align: center;
            font-size: 2rem;
            color: #333;
            margin-bottom: 20px;
        }

        /* Form Styling */
        form {
            display: grid;
            grid-template-columns: 1fr;
            gap: 15px;
        }

        form div {
            display: flex;
            flex-direction: column;
        }

        form label {
            font-size: 1rem;
            color: #333;
            margin-bottom: 5px;
        }

        form input, form select, form textarea {
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            background-color: #f9f9f9;
        }

        form input[type="date"], form input[type="time"] {
            width: 100%;
        }

        form textarea {
            resize: vertical;
            min-height: 100px;
        }

        /* Button Styling */
        button {
            padding: 12px 20px;
            font-size: 1rem;
            color: white;
            background-color: #4CAF50;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            margin-top: 20px;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #45a049;
        }

        /* Additional Styles for Select Input */
        select {
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            background-color: #f9f9f9;
        }

        /* Styling for Errors (if any) */
        form div.error input, form div.error textarea, form div.error select {
            border-color: #e74c3c;
            background-color: #fde4e4;
        }

        form div.error label {
            color: #e74c3c;
        }

        /* Styling for small text (like tooltips or descriptions) */
        small {
            color: #777;
            font-size: 0.875rem;
        }
    </style>
</head>
<body>
    <div class="content">
        <h1>Edit Order for Store ID: <%= storeId %></h1>
        <form action="/<%= storeId %>/orders/edit/<%= order.id %>" method="POST">
            <div>
                <label for="item">Item:</label>
                <input type="text" id="item" name="item" value="<%= order.item_name %>" required><br><br>
            </div>
            
            <div>
                <label for="code">Code:</label>
                <input type="text" id="code" name="code" value="<%= order.code %>" required><br><br>
            </div>
            
            <div>
                <label for="description">Description:</label>
                <textarea id="description" name="description"><%= order.description %></textarea><br><br>
            </div>
            
            <div>
                <label for="quantity">Quantity:</label>
                <input type="number" id="quantity" name="quantity" value="<%= order.quantity %>" required><br><br>
            </div>
            
            <div>
                <label for="units">Units:</label>
                <input type="text" id="units" name="units" value="<%= order.units %>" required><br><br>
            </div>
            
            <div>
                <label for="dateOrdered">Date Ordered:</label>
                <input type="date" id="dateOrdered" name="dateOrdered" value="<%= order.date_ordered %>" required><br><br>
            </div>
            
            <div>
                <label for="status">Status:</label>
                <select id="status" name="status" required>
                    <option value="">Select Status</option>
                    <option value="Pending">Pending</option>
                    <option value="Confirmed">Confirmed</option>
                </select>
                <br><br>
            </div>
            
            <button type="submit">Update Order</button>
        </form>
    </div>
</body>
</html>
